# Module 4 – HSV Color Segmentation

This module implements color-based segmentation using the HSV color space.

The notebook reads the image `Emerald_Lakes_New_Zealand.jpg`, converts it to HSV, creates a green mask, and segments the lakes using `bitwise_and()`.

## Notebook:
- CV_Mod4_Emerald_Lakes_HSV.ipynb
